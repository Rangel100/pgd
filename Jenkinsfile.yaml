pipeline {

  agent any
  environment {
    PRODUCTOR = 'Steven'
  }
  
  stages {
  
    stage("build"){
    
      steps {
        echo "building for ${PRODUCTOR}"
        echo 'building ...'
        
      }
      
    }
    
  }
  
}